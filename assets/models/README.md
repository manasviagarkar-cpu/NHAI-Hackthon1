# Model placeholder
# Run `npm run download-models` or `bash scripts/download_models.sh` to fetch all models.
# Expected files after download:
#   - mobilefacenet.tflite  (~4MB)  — Face embedding extraction
#   - blazeface.tflite      (~0.4MB) — Face detection
#   - face_landmark.tflite  (~8MB)  — 468-landmark face mesh
